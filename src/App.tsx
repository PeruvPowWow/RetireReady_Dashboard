export default function App() {
  return (
    <div className="min-h-screen grid place-items-center bg-slate-50 p-6">
      <div className="w-full max-w-lg rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h1 className="text-2xl font-bold text-slate-900">Tailwind is working ✅</h1>
        <p className="mt-2 text-slate-600">
          Next: we’ll paste the RetireReady dashboard (participant + admin policy + activity log).
        </p>
        <button className="mt-4 rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800">
          Test button
        </button>
      </div>
    </div>
  );
}
